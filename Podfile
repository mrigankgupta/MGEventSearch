# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'MGEventSearch' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for MGEventSearch
  pod 'Kingfisher'
  pod 'SwiftLint'

  target 'MGEventSearchTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'MGEventSearchUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
